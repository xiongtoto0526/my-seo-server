<!DOCTYPE html>
<html>

<head>
    <%- include('meta') %>
        <meta name="keywords" content="<%= article.keywords %>">
        <meta name="description" content="<%= article.description %>">
        <title>
            <%= article.title %>
        </title>
        <link rel="stylesheet" href="/styles.css">
        <link rel="stylesheet" href="/stackoverflow-light.min.css">
</head>
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f7f7f7;
    }

    .article-container {
        margin: 0 auto;
        padding: 20px;
        box-sizing: border-box;
    }

    .article-container .article-title {
        padding: 10px;
        margin: 0;
    }

    .article-container .article-title .name {
        margin: 0;
        padding: 10px;
        border-bottom: 1px solid #f0f0f0;
        background-color: #fff;
        font-size: 16px;
        font-weight: 600;
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
    }

    .article-container .article-title .text {
        padding: 10px;
        font-size: 16px;
        font-weight: normal;
        background-color: #fff;
        border-bottom-left-radius: 10px;
        border-bottom-right-radius: 10px;
    }

    .article-container .article-title .text pre {
        white-space: pre-wrap;
        word-break: break-all;
        padding: 10px;
    }

    .article-container .article-content {
        font-size: 14px;
        color: #666;
        line-height: 1.6;
    }
</style>

<body>
    <%- include('header') %>
        <main>
            <div class="article-container">
                <div class="article-title">
                    <p class="name"><%= __('page.article.title') %></p>
                    <div class="text">
                        <%= article.title %>
                    </div>
                </div>
                <div class="article-title">
                    <p class="name"><%= __('page.article.content') %></p>
                    <div class="text">
                        <%- article.content %>
                    </div>
                </div>
            </div>
        </main>
</body>